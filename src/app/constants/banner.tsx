

const AsciiBanner = () => {
    return (
        <pre style={{fontSize: '1rem', padding: '0px', margin: '0px'}}>
            {'->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->\n'}
            {'<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-\n'}
        </pre>
    );
};

export default AsciiBanner;